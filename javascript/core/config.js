export const config = Object.freeze({
  gridSize: 25,
  startSegmentCount: 5,
  startSpeed: 100,
  maxSpeed: 50,
  minSpeed: 150,
  colors: {
    snake: "red",
    apple: "green",
    background: "black",
  },
});
